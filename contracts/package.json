{
  "name": "confession-registry",
  "version": "0.1.0",
  "private": true,
  "description": "Confession Booth publication registry — minimal, immutable, tanpa plaintext.",
  "scripts": {
    "compile": "hardhat compile",
    "build": "hardhat compile",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "node": "hardhat node --port 8545"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.1.2",
    "@nomicfoundation/hardhat-ethers": "^3.1.3",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@types/chai": "^5.2.3",
    "@types/mocha": "^10.0.10",
    "chai": "^4.5.0",
    "ethers": "^6.17.0",
    "hardhat": "^2.29.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
